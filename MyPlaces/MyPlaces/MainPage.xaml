<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="MyPlaces.MainPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyPlaces"
             xmlns:behaviors="clr-namespace:MyPlaces.Behaviors">

    <StackLayout>
        <Entry Text="{Binding SearchTerm, Mode=OneWayToSource}">
            <Entry.Behaviors>
                <behaviors:EventToCommandBehavior EventName="TextChanged" Command="{Binding SearchCommand}" />
            </Entry.Behaviors>
        </Entry>

        <ListView ItemsSource="{Binding Places}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding Name, Mode=OneWay}" Margin="12,6,6,6" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>